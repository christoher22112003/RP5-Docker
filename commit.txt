Commit: Corrección de rutas dinámicas y mejoras en la generación de logs.

Cambios realizados:
1. Se corrigieron las rutas en todos los scripts (`1Smain.sh`, `2Smain.sh`, `install_docker.sh`, `verify_docker.sh`, `install_portainer.sh`, `install_pihole.sh`) para que sean relativas dinámicas, asegurando que funcionen en cualquier ubicación del sistema.
2. Se actualizó `verify_docker.sh` para generar logs en la carpeta `logs`.
3. Se actualizó `install_docker.sh` para generar logs en la carpeta `logs`.
4. Se creó una carpeta `logs` para almacenar los logs de errores generados por los scripts de instalación.
5. Se actualizó el archivo `steps.txt` para reflejar los cambios realizados.
6. Se documentaron los archivos que deben eliminarse manualmente.