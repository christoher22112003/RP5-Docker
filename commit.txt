Commit: Mostrar pasos internos en pantalla con color amarillo.

Cambios realizados:
1. Se actualizaron los scripts `install-docker.sh`, `verify-docker.sh`, `install_pihole.sh` y `install_portainer.sh` para que los pasos internos se reflejen en la pantalla con color amarillo.
2. Se verificó que todos los scripts generen logs en la carpeta `logs`.
3. Se documentaron los pasos realizados en el archivo `steps.txt`.