Commit: Verificar si los servicios ya están instalados antes de intentar instalarlos.

Cambios realizados:
1. Se actualizó el script `install_portainer.sh` para verificar si Portainer ya está instalado antes de intentar instalarlo.
2. Se actualizó el script `install_pihole.sh` para verificar si Pi-hole ya está instalado antes de intentar instalarlo.
3. Se actualizó el script `install-docker.sh` para verificar si Docker ya está instalado antes de intentar instalarlo.
4. Se documentaron los pasos realizados en el archivo `steps.txt`.